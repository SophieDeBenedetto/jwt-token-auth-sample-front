import { formatRegistrationErrors } from 'token-auth-sample-front/helpers/format-registration-errors';
import { module, test } from 'qunit';

module('Unit | Helper | format registration errors');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatRegistrationErrors([42]);
  assert.ok(result);
});
